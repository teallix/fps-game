import re
import sys

number = [-2, -1, 0, 1, 2]

def extract_positive(numbers):
    positive_numbers = []
    for number in numbers:
        if number > 0:  # Filtering condition
            positive_numbers.append(number)
        return positive_numbers
extract_positive(number)
class File:
    for number in number:
        if number > 0:
            re.error(5)
            sys.exit()
sto = 'an example word:cat!!'
match = re.search(r'word:\w\w\w', sto)
# If-statement after search() tests if it succeeded
if match:
    print('found', match.group()) ## 'found word:cat'
else:
    print('did not find')
if sto:
    re.search(r'cat\w\w\w', sto)
ste = 'm1m2m3m4m5'
right = re.search(r'm4\w', 'm5') ## find m4 and m5
if right:
    print('Found m4 and m5')
else:
    wrong = re.search(r'm1', 'm2') ## find m1 and m2
so = 'ex: watch_demolished_file_audio23'
find = re.search(r'file_audio23\w', 'demolished')
class Files:
    for audio_1 in re.search(r'w', 'r'):
        pass ## check back on
    for forget in so:
        pass ## check back on
if 'word:' in match:
    re.search(r'files/Game.py\w\w\w', 'wrong_error54')
if match:
    print(match.group())
    print(match.group(1))
    print(match.group(2))
# Open file
f = open('test.txt', encoding='utf-8')
# Feed the file text into findall(); it returns a list of all the found strings
strings = re.findall(r'some pattern', f.read())
print(re.sub(r'([\w\.-]+)@([\w\.-]+)', r'\1@yo-yo-dyne.com', str))
rt = (re.findall(r'ai\1, r\f', str))
open('FullGame.py', encoding='utf-8')